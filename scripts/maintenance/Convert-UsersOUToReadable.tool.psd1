@{
    Category = 'Setup / Maintenance'
    Label    = 'Build readable navigation reference (.md)'
    Order    = 81
    # Reads the AD structure dump, so it belongs with the other AD tools.
    Audience = 'Admin'
}
